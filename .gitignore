.venv/
__pycache__/
*.pyc
.env
.DS_Store
.streamlit/secrets.toml
*.egg-info/
.pytest_cache/
.cache/

# Your real sources may be private, paid, or copyrighted. Keep them out of the repo,
# especially before pushing to GitHub. The synthetic sample under sample_data/ stays tracked.
documents/
config/sources.yaml
# A real portfolio export dropped at the repo root (your holdings are private).
/portfolio.csv
/holdings.csv
# Runtime eval cases captured from expert corrections (may reference real figures).
# Anchored to the repo root so it does NOT also ignore the src/data/ package.
/data/
