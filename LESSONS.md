# LESSONS (append-only)
> Format: `date | what went wrong | root cause | corrective rule`. Read at session start.

2026-06-15 | python-dotenv was in requirements.txt but the venv install command hand-listed packages and omitted it; app crashed on import, caught by streamlit AppTest | hand-listing install packages instead of installing from requirements.txt | install with `pip install -r requirements.txt`, never hand-list; run AppTest (not just `streamlit run` health) as the F7 gate since health only proves the server booted
2026-06-18 | citation contract: is_verified_fact used any(primary) so a claim co-citing an annual report AND a finfluencer chunk passed as fact | reached for the permissive quantifier on a trust check | on a trust/safety gate, a single bad input must break the property: use all(), not any(); guard empty. Found by adversarial review, not by me first.
2026-06-18 | grounding: claims with no resolved citation still displayed as opinion; AMFI accepted nan/inf/0 NAV as a Tier-1 fact | did not push every input through the "no source -> not shown" rule | enforce the contract at every boundary: drop uncited claims entirely, reject non-finite/non-positive numeric facts at parse time
2026-06-18 | ingestion: build_library let one corrupt PDF crash the whole library; the app render `else` branch showed a FACT-that-lost-verification as bare unlabeled text; get_library cached sources with no invalidation | trusted batch inputs and upstream enforcement | per-item ingestion must degrade (try/except per file -> report, don't crash); the DISPLAY layer must fail safe (unknown/unverified -> warning, never bare text), not assume upstream validation ran; cache the trust config keyed on its fingerprint so de-trusting a source takes effect without restart. Found by adversarial review.
2026-06-15 | loader used set() for column candidates (nondeterministic priority -> could pick total "Cost" as per-share avg cost); float('nan') quantity slipped past the `<= 0` guard and poisoned all totals; negative avg_cost accepted silently | trusted happy-path CSV input; did not fuzz malformed columns/values | for money math, validate inputs hard: reject non-finite in number parsing, use ordered (not set) match candidates most-specific-first, guard sign; add one regression test per class of bad input. Found by adversarial review, not by me first.
2026-07-08 | src/data/nse_annual_reports.py (load-bearing, imported by app.py) was never committed; an unanchored `data/` .gitignore rule also matched the src/data/ package and hid it. Only surfaced when a new file there was refused by `git add`. A fresh clone / Streamlit Cloud deploy would have crashed on import | wrote `data/` instead of `/data/`, so the runtime-data ignore silently swallowed a source package | anchor directory ignores to the repo root (`/data/`), and before shipping run a fresh-checkout smoke (`git archive HEAD` -> AppTest) so an untracked import breaks the check, not the deploy.

<!-- BUILD metrics (signal, not target): 2026-07-08 | parv_corrections=0 | repeat_mistakes=0 | bugs_found=2 (untracked load-bearing file via gitignore bug; clumsy invest dataframe caught pre-commit) | shipped_first_try=6 (each phase passed verify.sh on first run) | rework_commits=1 (gitignore fix + rescued file). Adversarial self-review found the untracked-file bug via the fresh-checkout discipline, not after deploy. -->

<!-- BUILD metrics: 2026-07-08 (post-deploy iteration) | parv_corrections=1 (WhatsApp free tier isn't viable -> substituted ntfy, flagged honestly) | repeat_mistakes=0 | bugs_found=3 (fpdf multi_cell zero-width cursor; plain_points used unfetched median_pe -> price reason missing; screener bare-UA blocked on Cloud) | shipped_first_try=high (PDF, warning, screener-harden, CA-A/B/C/D/E each green after fix) | rework_commits=0. Also caught two self-made test-expectation errors (CAGR float-equality, ranking room=+1) before commit. Recurring lesson: verify metric wiring against REAL data live (the median_pe + trend bugs only showed on live symbols, not unit tests). -->

2026-07-08 | pushed a commit while a test was red: chained `./verify.sh | tail` then `git commit/push` with newlines, so the commit did not gate on verify.sh's exit (the pipe masked it) | assumed sequential commands imply gating; a pipe returns tail's exit code, not the script's | gate commits explicitly: `if ./verify.sh; then git commit && git push; fi` (never rely on `;`/newlines or a piped exit). The red test itself was test pollution: running the full app via AppTest in-process load_dotenv'd .env into os.environ and broke test_llm; snapshot/restore os.environ around in-process AppTest runs.

<!-- BUILD metrics: 2026-07-08 (full audit) | parv_corrections=1 (password-every-session is unreasonable -> URL-key auto-login) | repeat_mistakes=0 | bugs_found=9 via adversarial review (2 HIGH: allocation cap breach + AR grounding spoof; 4 MED; 3 LOW) all real, material ones fixed + regression-tested | shipped_first_try=mostly (one red-push lapse, caught + corrected same turn) | rework_commits=1 (env-isolation fix). Adversarial subagent found the 2 HIGH bugs I'd missed — the review earns its keep. 196 tests. -->

2026-07-09 | the 09:49 launchd daily run crashed on a single 20s ReadTimeout reading holdings from the Apps Script bridge; no picks produced that day | AppsScriptGateway._http_get/_post were single-shot requests with a tight 20s timeout and no retry, but Apps Script web apps cold-start slowly and this same bridge also serves the parents' picks read in the app | any network boundary a parent-facing feature depends on must retry transient failures with backoff and a cold-start-tolerant timeout; added _retry (3 attempts, backoff, injectable sleeper) + timeout=(10,45) to AppsScriptGateway, TDD'd (flaky-getter succeeds on 3rd try; exhausted-attempts re-raises). Verified live: re-run wrote 11 picks and the app read them back.

2026-07-09 | adversarial review of the four tab handlers found the primary "Research" button could crash the whole page: the annual-report text_provider (NSE auto-fetch AND the manual-URL override) called adapter.fetch (urlopen + pypdf) unguarded, and _extract called text_provider unguarded, so a timed-out / 403 / unparseable PDF (common from a cloud IP) threw a full-page stack trace at the parents | the "returns None if unavailable" contract was documented but not actually implemented — the fetch site had no try/except | honor abstain-on-failure at the boundary: wrapped text_provider in _extract and adapter.fetch in fetch_annual_report_text with try/except -> None; a report just abstains, never crashes. Same review: the Ask-tab verified-fact tick was tier-only (is_verified_fact checks all-PRIMARY) with NO numeric grounding, so an LLM misquote citing a real primary chunk would show a green ✓ — the exact wrong-figure-with-confidence failure the app exists to prevent (latent: unreachable for the parents' real-stock questions today since no real PRIMARY docs are deployed, only news). Added numbers_grounded() to _assemble_result: a FACT whose material number (>=3 digits, digit-normalized exact match) is absent from its cited chunk text is downgraded to UNVERIFIED. Bias is conservative on purpose (a wrongly-flagged true fact only shows "reported, not verified"; a false ✓ never happens). Also fixed reload button st.rerun() wiping its own st.error, and risk tiles showing a fabricated 0.00 on empty history.

<!-- BUILD metrics: 2026-07-09 (post-deploy live audit + resilience) | parv_corrections=0 | repeat_mistakes=0 | bugs_found=6 (1 real prod outage caught from the launchd log: daily timeout; adversarial review found 2 HIGH [Research-button AR crash, Ask ✓ numeric-grounding gap] + 2 MED [reload error-wipe, risk 0.00 display] + 2 acknowledged LOW) all fixed + regression-tested except the 2 LOW (documented acceptable) | shipped_first_try=high (each fix green on first verify.sh; gated commit with `if ./verify.sh`) | rework_commits=0. Live-verified the daily fix (wrote 11 picks, read back) and the crash guard (build_report_for_symbol survives a throwing AR source end-to-end). 201 tests. The adversarial subagent again earned its keep — found the Research-button crash I'd have shipped. -->

<!-- BUILD metrics: 2026-07-09 (continuous improvement loop, /loop 3m) | parv_corrections=0 | repeat_mistakes=0 | improvements+bugs=6 across 6 gated pushes: mobile tab-overflow (verified at 375px); ranking coarse-binary ties -> conviction tiebreaker (verdict_strength); trend signal parsed from UI prose -> structured; STRONG on 1 signal -> requires >=2 (over-confidence); Screener quarterly table misread as annual (silent conflict) -> FY-end-month detection; confidence HIGH with debt unverified -> capped MEDIUM | shipped_first_try=high (each green on first/second verify.sh; 1 expected bank-test update after the STRONG change, caught same turn) | rework_commits=0. Theme that paid off: hunting OVER-CONFIDENCE on thin/ambiguous public data is the highest-value scan axis for a real-money tool — three of six were "we claimed more certainty than the evidence supported". Tests 201 -> 216. -->

<!-- BUILD metrics: 2026-07-09 (loop iterations 6-12, /loop 3m) | parv_corrections=0 | repeat_mistakes=0 | improvements+bugs=7 across 7 gated pushes: margin-of-safety magnitude into ranking (not just CHEAP tier); news recency filter (stale != recent); even water-filling allocation (was greedy -> one-name concentration); adversarial review of accumulated changes found+fixed a HIGH (STRONG reachable via 2 non-solvency signals while debt unverified); Screener bare-year header resilience (MED from that review); median-P/E priced at real FY period-end not hardcoded 31 Mar (affects the MoS signal); annual-report points marked self-reported 📄 not cross-verified ✓ | shipped_first_try=high | rework_commits=0 | reverts=0. Adversarial subagent again caught a hole I'd DECLARED fixed (STRONG/debt) — verifying claims against code, not commit messages, is the load-bearing habit. Recurring highest-value axis stays OVER-CONFIDENCE (5 of 13 loop fixes). Backlog of obvious high-value items now ~exhausted; being explicit per-iteration when a change is marginal rather than manufacturing churn. Tests 216 -> 228. -->

<!-- BUILD metrics: 2026-07-25 (SPEC v4 W1 freshness engine, increment 1) | parv_corrections=0 | repeat_mistakes=0 | shipped src/freshness/ (append-only JSONL ingestion event log w/ content-hash dedup + version supersession as an event-sourced projection, never mutating a prior line; near-duplicate news clustering via overlap-coefficient token similarity + min-shared-token guard; as-of/staleness with undated!=fresh; news RSS wired end-to-end behind NewsSource) | TDD=yes (31 tests written red-first then greened) | shipped_first_try=high (one impl fix: normalize_title apostrophe should collapse without splitting the word, caught by its own test) | rework_commits=0 | tests 551 -> 582. Reject-hard-at-core + degrade-per-item-at-batch both honored (ingest() raises on empty content/ids/bad as_of; ingest_news() try/excepts per item and around the whole fetch). Note for review: worktree `.venv` symlink is NOT matched by the `.venv/` (dir-only) gitignore rule — staged files explicitly to avoid committing it. -->

<!-- BUILD metrics: 2026-07-25 (SPEC v4 W1 freshness engine, increment 2) | parv_corrections=0 | repeat_mistakes=0 | shipped: NSE/BSE corporate-announcement source (AnnouncementSource, injectable fetcher, PRIMARY tier, ToS-documented seam) + annual-report availability tracking (AnnualReportRef + NseAnnualReportResolver.latest_report, FY-end as-of) + filings_ingest (announcements + AR into the SAME event log, one logical AR record per symbol so a new FY supersedes) + scripts/ingest_freshness.py runnable scheduled entrypoint (thin run_ingest/parse_symbol_args/format_summary orchestration) + a regression closing the news_ingest errors-counter coverage gap (both bad-input classes) | TDD=yes (29 tests written red-first, confirmed ModuleNotFound/ImportError before impl, then greened) | shipped_first_try=high (green on first verify.sh after impl) | rework_commits=0 | tests 582 -> 611. Honored: reject-hard-at-core + degrade-per-item-at-batch (empty source_id/no-key -> errors++, batch survives), abstain-at-fetch-boundary (every fetcher/resolver try/excepts -> []/None/found=False), offline tests (no live NSE/Google hit per SPEC v4 §5 ToS), conservative as-of (FY-end errs older not fresher). latest_report_url refactored to delegate to latest_report — existing resolver tests stayed green. .venv symlink again untracked; staged files explicitly. -->

<!-- BUILD metrics: 2026-07-25 (SPEC v4 W4 orchestration, increment 1) | parv_corrections=0 | repeat_mistakes=0 | shipped: src/research/orchestrator.py — a PLAIN-PYTHON explicit orchestrator (locked decision: NO langgraph/crewai/autogen), explicit frozen-dataclass STATE threaded through discrete deterministic stages PLAN->RETRIEVE->COMPUTE->VERIFY(gate)->WRITE, each a pure-ish fn appending a StageTrace; COMPUTE pre-derives CAGR + latest-two-year YoY growth from typed records via computed_figures (compute-don't-generate); hard abstain GATE (empty retrieval / no LLM / derivation-with-no-records -> honest "insufficient verified evidence", never fabricated). Wired the W3 seam into the prompt: grounded_analyst gained write_answer(retrieved, computed_figures=()) + _build_user_prompt computed block (default empty -> BYTE-IDENTICAL, answer() delegates, all prior tests unchanged) | TDD=yes (16 tests red-first: ModuleNotFound/AttributeError confirmed before impl, then greened) | shipped_first_try=high (one test-setup fix: derivation-gate test hit the empty-retrieval arm; used matching prose) | rework_commits=0 | tests 664 -> 680. Adversarial fresh-context review found a MED-lean-HIGH real-money risk I'd only DISCLOSED not fixed: _derive_figures grouped by (company, source_doc, unit) could compose a CAGR/growth from TWO different rupee figures (revenue+profit) -> meaningless number as 'already computed'. FIXED not just disclosed: _single_record_years drops any fiscal year with >1 record in a group (ambiguous which figure), so a multi-figure chunk can never seed a mixed series; added a regression reproducing it (asserts computed==()). Residual narrow case (each year a different single figure) is irreducible without a figure label, disclosed, and downgraded by numbers_grounded anyway. Honored: conservative withhold-when-unsure, reuse-over-duplicate (cagr delegated, _period_year reused not re-regexed), no app.py touch, no UI wiring (that is W7). .venv symlink untracked; staging files explicitly. -->

<!-- BUILD metrics: 2026-07-25 (SPEC v4 W3 compute-don't-generate, increment 1) | parv_corrections=0 | repeat_mistakes=0 | shipped: record-backed numeric grounding (numbers_record_backed + a find_record-over-numeric_records(hits) gate in _assemble_result — a FACT/OPINION material number that only substring-matches the prose but resolves to NO typed record is downgraded to UNVERIFIED; a bare-year phantom "2024 crore" is the canonical catch) + structured ingestion enabled at the grounded-analyst ingestion paths (library.build_library, annual_report_reader.read_filing) so scaled-table cells become typed records + src/research/computed_figures.py compute-don't-generate seam (ComputedFigure; yoy_growth_pct/ratio_pct primitives, growth_between/margin_between typed-record adapters, CAGR delegated to analysis.trends.cagr — reuse not a second copy) | TDD=yes (15 tests red-first: ImportError/ModuleNotFound confirmed before impl, then greened) | shipped_first_try=high (green on first full verify.sh after impl) | rework_commits=0 | tests 649 -> 664. Honored: conservative bias (check only ever DOWNGRADES, gated on the corpus having any typed record so record-less prose keeps the existing numbers_grounded contract; a wrongly-flagged true fact shows "reported, not verified", never a false green tick), money-math guards (divide-through-zero / non-finite -> None, never a fabricated or infinite value), reuse-over-duplicate (cagr delegated). Self-caught before running: a CAGR test used 100/121/133.1 expecting 10%/yr — CAGR reads only endpoints, so 10%/yr over 3 yrs is 100/110/121; fixed the expectation, not the math. Verified the verified_figures PRIMARY-doc path does NOT lose green ticks (its figures format as ₹..crore / ..% / ..x, all recognized as records under blind ingestion). Compute module is the tested SEAM, not yet wired into the LLM prompt (that is W4 orchestration). Did NOT touch app.py. .venv symlink untracked; staged files explicitly. -->



2026-07-25 | W5 eval: crore/lakh unit trap passed BOTH digit-only numeric checks (numbers_grounded + numbers_record_backed match on digits, scale word dropped) -> a 100x error slipped through | the record-backed check resolves a claim number to a record by digit-key only, ignoring the scale word | added numbers_unit_consistent (parses the claim's own scale via extract_records, downgrades when every same-digit record disagrees on crore/lakh/million); found while BUILDING the red-team suite, not after. Also: red-team + unit-normalized numeric-exact-match now gate run_eval.py (3 gates, non-zero exit). Residuals (honest): period-mixing only caught at the typed-record/exact-match layer; staleness flagged by the freshness primitive but not yet inlined into the rendered answer (W7).

<!-- BUILD metrics: 2026-07-25 (SPEC v4 W7 UI, increment 1 — functional integration + trust UI) | parv_corrections=0 | repeat_mistakes=0 | shipped: FIRST app.py wiring of the W4 orchestrator into the Ask tab (grounded-answer path now PLAN->RETRIEVE->COMPUTE->VERIFY[gate]->WRITE, compute-don't-generate end to end) with a degrade-to-grounded.answer fallback on any orchestrator error; four additive, each-try/except-wrapped trust-UI elements per answer claim — claim-type BADGE (st.badge, green ONLY for a verified fact), click-through SOURCE SPANS (source·locator + exact supporting quote from Claim.spans()), FRESHNESS banner (describe_freshness off the news-locator date -> stale/undated visibly flagged), SHOW-THE-COMPUTATION expander for any ComputedFigure (label+value+inputs+formula) | TDD=yes (5 new tests incl. an AppTest rendering the badge through Streamlit's own runtime; pure helpers claim_badge/claim_freshness_lines/format_computed_figure tested directly, the repo's app-fn pattern) | shipped_first_try=high (green on first full verify.sh after impl) | rework_commits=0 | tests 715 -> 720, run_eval EVAL GATE PASS. PRIME DIRECTIVE honored (real money, live app): every new data access wrapped so a failure renders a small note, never a page crash; password gate + all four tabs + MF/SIP + disclaimer untouched; the app-smoke (no LLM, full render) stays exception- and error-free. Adversarial self-check: ran the REAL orchestrator with a fake LLM over a 3-year rupee series and rendered its actual claims+computed figure through the exact Ask block — no exception; confirmed the badge NEVER shows green for a computed result phrased as its value (the derived number isn't a literal source substring, so the existing conservative numeric grounding downgrades it to estimate/unverified — faithfully surfaced, never a false green tick). Verify env note: the worktree had NO .venv, so verify.sh silently fell back to a python3 without deps (its `$?`-after-pipe masked the failure); symlinked .venv -> main venv (dir-only .venv/ gitignore does NOT match the symlink; left untracked, staged files explicitly). Did NOT change grounding/orchestration logic (out of scope for this increment). Visual redesign is increment 2. -->

<!-- BUILD metrics: 2026-07-25 (SPEC v4 W6 span-level citations, increment 1) | parv_corrections=0 | repeat_mistakes=0 | shipped: Citation gained an OPTIONAL span-level supporting `quote` (defaults "" -> every existing 3/4-positional construction stays valid) + Citation.as_span()/Claim.spans() exposing {source_id, locator, quote} for W7 click-through; grounded_analyst gained supporting_span (numeric-fragment-first, key-phrase overlap fallback, NEVER quotes back an absent figure) wired into _assemble_result (each resolved citation carries the exact fragment inside ITS cited chunk); citation-quality gate citation_supports_numbers ("cited but not verified", SPEC v4 §3) as an explicit downgrade arm after numbers_grounded | TDD=yes (13 tests red-first: ImportError confirmed before impl, then greened) | shipped_first_try=high (green on first full verify.sh after impl) | rework_commits=0 | tests 702 -> 715, run_eval exit 0. HONEST on the downgrade's teeth: citation_supports_numbers is EQUIVALENT-BY-CONSTRUCTION to numbers_grounded for claims with material numbers (fragment-union of number keys == whole-text keys; verified 0/4000 disagreements on a randomized property check), so it adds ZERO regressions AND zero NEW downgrades today -- its net-new value is the per-citation span payload a UI needs + an explicit structural "green fact => a citation whose quote actually contains the figure" invariant (defense-in-depth if numbers_grounded is ever weakened). Conservative bias preserved (only ever downgrades; supporting_span's fallback fragment cannot contain the claimed-but-absent number, so a wrong figure is never quoted back). Did NOT touch app.py. .venv is a worktree symlink to the main venv (not matched by the dir-only .venv/ gitignore); staged files explicitly. -->


2026-07-25 | W8 compliance: SEBI assigns the operator full responsibility for AI output and bars unregistered advice/return/win-rate marketing (PR Sundar/Baap of Chart/Avadhut Sathe impounds) | a research tool can drift into advice phrasing in its OWN copy over time, and a human diff-read is not a reliable guard | added src/compliance/lint.py (ast-scoped to static literals + system prompt + disclaimers, clause-level negation guard so labels/quotes/negated-disclaimers do not false-positive) as a 4th run_eval gate; AI_DISCLOSURE in footer + beside each answer + on PDF export; conservative false-negative bias, human review + DRAFT->APPROVED stay the backstop.

<!-- BUILD metrics: 2026-07-25 (H2 period-aware retrieval, closes the W5 period-mixing residual) | parv_corrections=0 | repeat_mistakes=0 | shipped: numbers_period_consistent (grounded_analyst) — a claim-time downgrade guard mirroring numbers_unit_consistent; when the QUESTION targets a fiscal year (Plan.target_period via the shared structure.detect_period, threaded orchestrator._write -> write_answer -> _assemble_result, and also into the direct answer() path), a material figure resolving ONLY to records of a DIFFERENT explicit period is downgraded to UNVERIFIED. Period compared by 4-digit year (reused computed_figures._period_year, no 4th FY parser). Added a 5th red-team case (_case_period_claim_leak: an FY2024 question answered with FY2023's 500-crore figure through the REAL orchestrator) + run_eval now 5/5 | TDD=yes (14 tests red-first: ImportError/missing-case confirmed before impl, then greened — 8 guard unit tests + 5 orchestrator integration + 1 red-team) | shipped_first_try=high (green on first full verify.sh after impl) | rework_commits=0 | tests 770 -> 784, run_eval EVAL GATE PASS exit 0. Conservatism HONORED (real money): the guard only ever DOWNGRADES (never a false green tick), an UNTAGGED record (unknown period != wrong period) is ALWAYS acceptable so untagged data is never withheld, retrieval ranking + the COMPUTE record set are UNTOUCHED (a multi-year CAGR/growth derivation is never blinded), and with no target year the guard is inert (byte-identical behavior — proved by test_no_period_in_question_leaves_the_same_figure_verified_unchanged: SAME store + SAME claim verifies exactly as before). Residual disclosed honestly: (a) only explicit FY-style periods are targeted (matches detect_period; a bare-year "in 2024" is inert — under-targeting = safe, no coverage loss); (b) a comparison/growth question that NAMES one target year AND whose model restates the OTHER year's raw figure in the same claim would downgrade that claim to "reported, not verified" — a coverage nuance, but the safe direction (labelled, never hidden, never a false green tick), and not the point-in-time case this increment targets. Did NOT touch app.py (its orchestrator path is now period-aware for free). .venv is a worktree symlink to the main venv (NOT matched by the dir-only .venv/ gitignore); staged files explicitly. -->

2026-07-25 | H2: W5 left an honest residual — period-mixing was caught ONLY at the typed-record/exact-match layer, so an FY2024 question answered with FY2023's figure passed the digit-only claim-time checks (numbers_grounded/record_backed/unit_consistent all ignore period) and would render as a verified FY2024 fact | the claim-verification stack had unit-awareness but no period-awareness; the record's true period was preserved but never CHECKED against what the question asked | added numbers_period_consistent + Plan.target_period (SPEC v4 §2.2 metadata filtering), downgrading a figure backed only by a different explicit period; conservative (untagged never excluded, inert with no target year, compute/retrieval untouched). Red-team now proves it at CLAIM time through the real orchestrator, not just the record layer.

2026-07-26 | H7 freshness delivery: the snapshot's "recent" count summed new+superseded+skipped, which INCLUDES undated items (an undated item is kept -> ingested -> lands in those counters AND in `undated`), while the banner labels the count "from the last N days" | a count meant as "known-dated recent coverage" was built from counters that also carry undated items, so the parents' page would claim e.g. "1 news from the last 120 days" for an item kept precisely because its date is unknown | subtract `undated` in snapshot_for so the count is KNOWN-DATED-recent only; regression test asserts an all-undated run yields 0. Found by the adversarial fresh-context review, confirmed against the mirrored ingest tests (undated==1 AND new==1 for the same item). Second review find (hardened): Google Sheets can return a date-typed cell as an ISO timestamp -> from_row now strips to the leading YYYY-MM-DD so the banner doesn't silently vanish on Cloud; residual (TZ-shift on a coerced cell) needs the one live round-trip check, documented in DEPLOY.md §4d.

<!-- BUILD metrics: 2026-07-26 (H7 freshness snapshot -> deployed app) | parv_corrections=0 | repeat_mistakes=0 | shipped: src/freshness/snapshot.py (SymbolSnapshot + snapshot_for projection, tolerant from_row incl. ISO-timestamp date strip, parse_snapshot) + scripts/ingest_freshness.py --publish (build_snapshot/publish_snapshot/gateway_from_env/resolve_publish, load_dotenv in main, best-effort publish never aborts ingest) + app.py load_freshness_snapshot (cached 30m, degrades to {}) + freshness_snapshot_line + Research-tab 🔄 banner (st.warning when overdue) + DEPLOY.md §4d launchd cron | TDD=yes (23 tests red-first then green, incl. an AppTest rendering the banner through Streamlit's runtime with the Sheets read injected at the src-level gateway) | bugs_found=3 via adversarial review (1 MED honesty: undated inflating the "recent" count -> FIXED; 1 MED functional: Sheets date coercion silently hiding the banner -> HARDENED + residual documented; 1 LOW: overdue flag as grey caption not warning -> FIXED) + 1 self-caught in build (cross-test @st.cache_data contamination made the AppTest pass alone but fail in the full suite -> clear caches before the run) | shipped_first_try=high (verify.sh green each impl pass; the review fixes added 3 tests) | rework_commits=0 | tests 818 -> 844. Zero server-side change: the Apps Script bridge already does generic tab read/write, so freshness rides the existing Sheets backend. Additive: no --publish / no snapshot -> ingest + app byte-unchanged. HONEST residual: the live Mac-cron -> real-Sheet -> Cloud-read round-trip is offline-unverified (needs the owner's Sheet URL/token + launchd install); NSE/BSE datacenter-IP block is why this runs on the Mac at all. .venv worktree symlink untracked (dir-only .venv/ gitignore misses it); staged files explicitly. -->

2026-07-26 | LIVE verification of H4 (BSE announcements) + H5 (PDF table extraction) on real data from the residential-IP Mac (user asked to verify, not just trust the offline tests) | H4: the BSE AnnGetData endpoint is REACHABLE (cookie-prime + Referer works, not IP-blocked) and the parser degrades safely, BUT the documented query returns the 18-byte sentinel "No Record Found!" for a known active filer (RELIANCE scrip 500325) across 30/180/365-day windows and every strType/strSearch variant tried -> the BSE fallback's query params look STALE and it is NOT confirmed to deliver live records. NSE announcements DO flow live (14 in-window for RELIANCE), so BSE is an unverified secondary, not a coverage hole. H5: the pipeline RUNS on the real 11MB/187-page RIL FY2026 AR (no crash, ~5s/40pp, degrade-safe) and pdfplumber-detected tables DO become typed records with scale (million/lakh/crore) end-to-end -- but QUALITY is rough: borderless tables mis-merge columns (the documented limitation, now confirmed real: four "'000 Tonnes" cells collapsed into one), duplicate header rows, non-rupee quantities (tonnes/counts) mis-tagged unit=rupees by the scale heuristic, and NO clean income-statement/balance-sheet surfaced as a structured table in the scan window (borderless financial tables fall to prose) | corrective: (a) BSE query params need validating/repairing against BSE's current AnnGetData API before the BSE source is trusted (spawned as a follow-up); (b) H5 table extraction is a best-effort improvement over pypdf ONLY where pdfplumber detects a ruled table, NOT a reliable precise-figure extractor for real Indian ARs -- so keep the cross-verify-or-withhold gate as the real guard (a rough cell degrades to UNVERIFIED, never a confident wrong figure); (c) a non-rupee "lakh/million" quantity can be mis-unit'd as rupees -- latent today (no PRIMARY AR docs deployed, only news) but a real unit-context guard is the fix if AR docs are ingested. The safety invariant HELD throughout: rough extraction degrades to unverified, never a confident wrong number.

2026-07-26 | FIX (BSE query, resolves the same-day finding above): the BSE fetcher used the dead endpoint AnnGetData/w with a capital-S strScrip and no subcategory, so it returned "No Record Found!" for every query | the working endpoint (per the maintained BennyThadikaran/BseIndiaApi client, live-confirmed) is AnnSubCategoryGetData/w with subcategory=-1 and a LOWERCASE strscrip | switched the endpoint + params in BseAnnouncementSource._http_fetch; live-verified BseAnnouncementSource().fetch("500325") now returns 15 real RELIANCE announcements (dated, categorized, with attachment URLs), parsed cleanly by the EXISTING parser (its NEWSSUB/CATEGORYNAME/NEWS_DT/ATTACHMENTNAME/SCRIP_CD mapping was right all along -- confirmed present in the live payload). Added a network-free regression that captures the constructed URL and asserts the AnnSubCategoryGetData endpoint + subcategory=-1 + lowercase strscrip, so a future edit can't silently revert to the dead endpoint. Lesson generalized: an exchange JSON endpoint that responds 200 with a "No Record Found!" sentinel for ALL inputs is a stale-query smell, not an empty-data truth -- verify against a maintained client, not the documented-but-aged shape.

<!-- BUILD metrics: 2026-07-26 (BSE announcements query fix) | parv_corrections=0 | repeat_mistakes=0 | shipped: BseAnnouncementSource._http_fetch endpoint fix (AnnGetData/w -> AnnSubCategoryGetData/w, +subcategory=-1, strScrip -> strscrip) + a network-free URL-capture regression test | verification=live (fetch('500325') -> 15 real records) + verify.sh 844 -> 845 green + run_eval 4-gate PASS | bugs_found=0 new (this IS the fix for a prior live-verification finding) | shipped_first_try=high (found the working shape from the maintained client on the first web lookup, confirmed live first try) | rework_commits=0. Parser needed NO change -- the earlier "field mapping unverified offline" note is now live-verified. No app.py touch (BSE is a freshness-ingest source, wired via scripts/ingest_freshness.py). -->

<!-- BUILD metrics: 2026-07-26 (wire BSE into the freshness ingest) | parv_corrections=0 | repeat_mistakes=0 | shipped: src/data/bse_scrip_codes.py (BseScripResolver: live-verified static seed of 31 holdings + a degrade-safe live PeerSmartSearch fallback with EXACT-ticker matching, cached hits AND misses) + run_ingest bse_source/bse_resolver pass (BSE announcements into the SAME log, degrade-per-symbol) + SymbolFreshness.bse_announcements + format_summary BSE segment + snapshot_for announcements_recent = max(NSE,BSE) not sum (same filing disclosed to both exchanges -> summing double-counts) + main() enables BSE by default + DEPLOY.md §4d note | TDD=yes (13 tests red-first then green: 8 resolver + 3 run_ingest BSE-pass + 2 snapshot max) | verification=LIVE: ingest --window-days 45 RELIANCE+ICICIBANK pulled 15 + 32 real BSE announcements into the log (47 bse_announcements events, dated, real titles) alongside NSE; resolver validated live 30/32 exact-ticker + BSE Ltd(543233) seeded (search returns BSELALGO) + MBAPL safely None (not on BSE) | verify.sh 845 -> 858 + run_eval 4-gate PASS | bugs_found=0 | shipped_first_try=high | rework_commits=0. SAFETY: the live resolver NEVER guesses -- exact ticker or None (a wrong scrip would ingest another company's filings); every boundary degrades (unresolved symbol / dead BSE feed / junk HTML -> None/[], NSE still covers). Additive: no bse_source/bse_resolver -> run_ingest + snapshot byte-unchanged (existing callers/tests green). No app.py touch (BSE feeds the ingest, surfaced via the existing H7 snapshot banner). -->

2026-07-26 | BSE-wiring adversarial review (fresh context) found a MED real-money hazard: the scrip-search parser regex `liclick\('(\d+)'...<span>(.*?)</span>` (re.S) paired a liclick scrip with the FIRST following <span> anywhere in the payload, so a span-less <li> would let its scrip pair with the NEXT row's span -> an exact-ticker "hit" returning a DIFFERENT company's scrip (invariant-1 breach: would ingest another company's filings) | the regex crossed <li> boundaries; the exact-ticker guard checks the ticker but not that scrip and ticker came from the same row | FIXED: parse each <li> in isolation (split on the tag) AND cross-check the liclick scrip against the span's trailing scrip token; a malformed/span-less row is skipped, never guessed. Regression tests cover the span-less cross-pairing + the scrip/span mismatch. Re-verified live (RELIANCE/ICICIBANK/ASTRAL resolve, BSE->None). Also (LOW from same review): BSE fetch hard-coded a 30-day server window while ingest runs a 120-day recency window -> widened the BSE fetch to _BSE_FETCH_WINDOW_DAYS=120 so BSE covers the same range as NSE (client-side is_recent still honors a smaller --window-days). Exposure was latent (all 31 holdings are seeded, so only an unseeded live-resolved symbol could hit the parser bug), but hardened before relying on the live fallback.

<!-- BUILD metrics: 2026-07-26 (professional UI overhaul) | parv_corrections=0 | repeat_mistakes=0 | shipped: premium design system (_IER_CSS: --ier-* tokens, elevated cards w/ real shadows, refined hover-lift buttons, a clean underline tab bar with plain-text labels [removed the emoji tabs], card-style expanders, tighter type scale) + a custom color-coded holdings table (holdings_table_html pure builder + render wrapper w/ st.dataframe fallback; symbol+sector stacked, right-aligned tabular nums, P&L% green/red, weight micro-bar, sorted by value, hide-sm mobile columns so 375px never overflows) + format_rupees_precise (2-decimal per-share prices so ₹15.62 isn't a rounded ₹16; integer quantities) + plotly chart theming (transparent bg for light/dark, app palette, theme-neutral text) | TDD=partial (6 holdings-table-builder tests + 1 formatter test written after the visual pass; app-smoke covers the chart path) | verification=LIVE in the local Streamlit preview (desktop light + dark + 375px mobile + a real ADANIPOWER research report render) | bugs_found=1 self-caught (a `price()` helper name collided with a `price` script variable at line 1453 -> the module-level function was shadowed by a float at runtime; the builder unit test caught the TypeError before it shipped -> renamed to call format_rupees_precise directly) | verify.sh 860 -> 867 + app smoke clean | rework_commits=0. Behaviour unchanged (research-only, all tabs/features intact); every custom render try/except-guarded so a markup failure falls back to the native widget, never a crash or a lost number. Lesson: writing the pure-builder test is what surfaced the name-collision a visual check missed -- extract render markup into a pure fn and unit-test it even for 'just CSS/HTML' work. -->

2026-07-26 | the UI push deployed an ImportError to the parents' LIVE app: app.py imports a NEW symbol (format_rupees_precise) from a changed src module, and Streamlit Cloud's code-only redeploy RERAN app.py without reimporting src.formatting (the old module stayed in sys.modules), so `from src.formatting import format_rupees_precise` raised on Cloud even though it worked locally and in every test | Cloud's fast redeploy on a code-only push reuses the process; `from X import newname` on an already-cached module doesn't reload X, so a newly-added symbol is missing until a full process restart | ANY deploy that adds a new src symbol imported at app.py top-level must be paired with a requirements.txt touch (bump a comment) to force a full env rebuild + fresh process. Saw the identical symptom locally earlier the same session (fixed with a server restart) and STILL shipped it to Cloud without forcing a rebuild -- the local restart should have been the tell. Corrective rule: after any push that adds a top-level import of a new/renamed src symbol, bump requirements.txt in the SAME push (or immediately verify the deployed gate renders, not just that the build 'succeeded').
