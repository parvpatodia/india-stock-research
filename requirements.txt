# Python runtime is pinned in runtime.txt (python-3.12). The versions below are the exact set
# verified in a clean local venv; keep them in sync when bumping so Streamlit Cloud builds match
# what was tested locally. WHY: reproducible, deterministic Cloud builds for a real-money app.
# Rebuild marker 2026-07-26b: a code-only push that adds a NEW src symbol imported by app.py can
# stale-import on Cloud (the process reruns without reimporting the changed src module); bumping
# this file forces a full env rebuild + fresh process so the new symbol resolves. See LESSONS.
streamlit==1.58.0
yfinance==1.4.1
pandas==3.0.3
lxml==6.1.1          # pandas.read_html parser for the Screener source; pandas 3.0 needs an explicit parser
plotly==6.8.0
litellm==1.89.2
pypdf==6.14.2
pdfplumber==0.11.10  # H5: geometry-aware PDF TABLE extraction (pulls pdfminer.six + pypdfium2 prebuilt wheel; Pillow already present). pypdf stays as the degrade-safe fallback.
python-dotenv==1.2.2
pyyaml==6.0.3
gspread==6.2.1
requests==2.34.2
fpdf2==2.8.7
pytest==9.1.0
