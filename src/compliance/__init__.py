"""Compliance surface (SPEC v4 §6, SEBI-load-bearing).

The system is research-only. It must never, in its OWN voice, give a buy/sell/hold call, promise a
return, or claim a win-rate/accuracy edge, and it must disclose that it is AI-assisted and
human-reviewed. `lint` enforces the first half in code so a future edit that introduces advice
phrasing fails the build.
"""
